# Application Configuration
PORT=3000
NODE_ENV=production

# Database Configuration (already configured in docker-compose.yml)
# Only change this if you want to use a different database
DATABASE_URL=postgresql://cvify:cvify_password@postgres:5432/cvify_db

# Groq Configuration
GROQ_API_KEY=your_groq_api_key_here